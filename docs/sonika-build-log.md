18/05/2026: Positioning locked, domains bought, brand claimed, first post live.
19/05/2026: Created sonika-outreach spreadsheet.
22/06/2026: Designed multi-tenant data model + RLS (agencies → sub_accounts → seats → call_logs). Written into architecture.md / decisions.md. Next: create Supabase project + apply migration.
25/06/2026: Built the foundation slice — agency auth + multi-tenant DB. Added @supabase/ssr wiring (browser/server/admin clients), Next 16 `proxy.ts` (renamed Middleware) for session refresh + route protection, supabase/ local project + `migrations/20260625120000_init.sql` (full schema, table GRANTs, RLS, and `handle_new_user` bootstrap trigger that creates agency+owner profile on signup). DAL (`requireProfile`), signup/login server actions (zod-validated), branded auth pages, protected /dashboard shell.
  Verified live (local Supabase via Docker): migration applies clean; real GoTrue signup fires the trigger → agency + owner profile created; two-tenant RLS test — Acme sees only Acme, Beta only Beta, service_role sees both; full browser e2e (Playwright) — signup → /dashboard greeting + agency name, session persists on reload, sign-out → /login, /dashboard blocked when signed out. `tsc` + `next build` green.
  Gotcha caught in verification: a fresh migration's tables have NO role grants, so authenticated/service_role queries 500 with "permission denied" even when RLS would allow — added explicit GRANTs to the migration. RLS gates rows; GRANTs gate table access; need both.
  Env gotcha: this machine's WSL service was Disabled (`Wsl/0x80070422`), blocking Docker — re-enabled `WslService` (admin) to unblock `supabase start`.
  Next: sub-account CRUD (create end-client from website URL), then the provisioning flow.
26/06/2026: Built sub-account CRUD (create/list/delete end-clients). Server actions `createSubAccount` + `deleteSubAccount` (src/app/dashboard/actions.ts) — auth re-checked via requireProfile inside each (Server Actions are POST-reachable), zod-validated, website normalized (bare domains get https://, rejects no-dot hosts). Insert stamps agency_id explicitly; RLS `with check` + delete scoping mean a forged/foreign id can't cross tenants — no manual agency filter on delete. UI: AddClientDialog (client modal, useActionState, closes on ok), DeleteClientButton (form-bound action + client confirm), dashboard reworked to list rows with status badges + website host + empty state.
  Verified live (Playwright, two agencies): invalid-URL rejected; create 2 → list shows both with draft badges + host link; reload persists; delete removes only the targeted row; second agency sees 0 (RLS isolation holds for sub_accounts too). tsc + build green.
  Next: provisioning flow — paste URL → Claude system prompt → Vapi assistant → Twilio number → seat live (start mocked; route all Claude/Vapi/Twilio server-side per architecture.md).
26/06/2026 (pm): Built the provisioning flow (the magic moment). Three server-side service modules (src/lib/services/): claude.ts is the ONE Claude service per CLAUDE.md — generateSystemPrompt, mocked by default, real @anthropic-ai/sdk Messages call (model claude-opus-4-8, env-overridable) behind ANTHROPIC_API_KEY; vapi.ts (createAssistant) + twilio.ts (provisionNumber) mocked with clean real-path seams (next integration). Orchestrator `provisionClient` (src/app/dashboard/actions.ts): RLS read of the sub-account = authorization, then service-role writes per architecture.md — upserts the 1:1 seat (reuses a failed/pending seat so it doubles as retry; refuses if already live), runs Claude→Vapi→Twilio, flips seat=live + sub_account=active; on any failure sets seat=failed + sub_account=draft (retryable). UI: ProvisionButton island (per-row, useActionState, pending/error, Retry variant) + dashboard now joins seats and shows green-pulse + number when live, "add website to provision" hint otherwise.
  Verified live (Playwright + DB): provision happy path — draft client → click → row shows +1555… number + active badge; DB confirms seat live with mock assistant id, number, and a stored 717-char system prompt; 1:1 seat per sub-account holds; two agencies each provisioned only their own (RLS isolation); no-website client correctly blocked with the hint. tsc + build green. NOT exercised: the failure→Retry path (no way to force a mock failure) — code path is straightforward but unverified.
  Consulted the claude-api skill before writing the Claude integration (model id + Messages shape). Next: real Vapi/Twilio integration (swap the mocks), then call logs (Vapi webhook → call_logs) and per-seat Stripe billing.
26/06/2026 (eve): Swapped the Vapi + Twilio mocks for real API integrations (fetch-based, no new deps), still env-gated so absent keys = mock (local dev unchanged). Researched exact shapes via the vapi-docs MCP first. Flow is now explicit 3-step in provisionClient: createAssistant (Vapi POST /assistant — openai/gpt-4o + 11labs voice + deepgram, all env-overridable) → purchaseNumber (Twilio AvailablePhoneNumbers search → IncomingPhoneNumbers buy, Basic auth) → registerTwilioNumber (Vapi POST /phone-number/import, routes the DID to the assistant). Added best-effort teardown: a failure after partial provisioning releases the Twilio number + deletes the Vapi assistant so nothing is orphaned/billed. .env.example documents VAPI_* / TWILIO_* (incl. that the SID starts with AC and AUTH_TOKEN is required).
  Verified: Vapi half LIVE against the real API with the user's key — create assistant 201 + delete 200 (reversible probe, no orphan). tsc + build green. NOT verified: Twilio purchase — creds in .env.local are incomplete (TWILIO_ACCOUNT_SID is not AC-prefixed and TWILIO_AUTH_TOKEN is absent), and a live buy costs money, so it's held pending valid creds + explicit go-ahead. The browser e2e was deliberately NOT run (would hit real APIs).
  Note: with VAPI_API_KEY set, clicking Provision now makes real Vapi calls; with the current incomplete Twilio creds it fails at the Twilio step and auto-tears-down the assistant (leaves a retryable failed seat).
27/06/2026: Completed live verification of the full Vapi+Twilio chain with real creds. Caught a real bug: the vapi-docs example for importing a Twilio number is stale — the live /phone-number/import API rejects `provider`/`number` and requires `twilioPhoneNumber` (E.164). Fixed registerTwilioNumber to send `{twilioPhoneNumber, twilioAccountSid, twilioAuthToken, assistantId}`. Re-verified end-to-end against the real APIs: create assistant 201 → buy Twilio DID 201 → import 201 with the number correctly routed to the assistant (assistantId matches) → teardown clean (Twilio release 204, Vapi phone-number + assistant deleted 200). No orphaned resources. tsc + build green. (Verification bought + immediately released 2 throwaway US numbers, ~$2 of Twilio charges total.) The provisioning flow is now real and working end to end. Next: call logs (Vapi end-of-call webhook → call_logs) and per-seat Stripe billing.
27/06/2026 (b): Added a PROVISIONING_MOCK kill-switch (src/lib/services/mode.ts → isMockProvisioning). When set (1/true/yes) the whole chain (Claude/Vapi/Twilio) uses mock paths even with live keys present — so live keys can stay in .env.local for convenience while dev never spends. Real provisioning (buys numbers) only runs when it's unset/0. Wired into all three service modules' mock gates + teardown helpers; documented in .env.example; set PROVISIONING_MOCK=1 in .env.local so dev is safe by default. Decided against Twilio test credentials: their magic-number-only model doesn't fit our search→buy flow and produces a fake number Vapi can't really import/route, so it wouldn't validate the step that matters — the mock covers cost-free dev better. tsc + build green.
27/06/2026 (c): Twilio account architecture decision — Sonika gets its own TOP-LEVEL Twilio project (sibling to GCA, not a subaccount: Twilio subaccounts can't nest, and Sonika must stay a potential parent for per-agency subaccounts in v2). MVP keeps numbers flat in the Sonika project; tenancy stays in Postgres. Switched Twilio auth from the master Auth Token to a scoped API Key (SK… + secret): twilio.ts Basic auth now uses TWILIO_API_KEY_SID:TWILIO_API_KEY_SECRET, and vapi registerTwilioNumber sends twilioApiKeySid/twilioApiKeySecret (confirmed via vapi-docs that import accepts API-key creds, so the Auth Token is dropped end-to-end). Account SID still required (URL path + Vapi). .env.local/.env.example updated; TWILIO_AUTH_TOKEN removed from code (grep clean). tsc + build green. NOT yet live-verified: the API-key path (need an SK key created in the Sonika project) — re-run the buy→import→release probe once it's pasted. Use a Standard key (sufficient for number search/buy/release; Main only needed later for subaccount creation).
27/06/2026 (d): API-key verification attempt — the Sonika Twilio project's auth/policy story. The created SK keys 401'd ("Authenticate") on /Accounts.json = SID/secret mismatch (couldn't pair them; deferred). Wired an Auth-Token FALLBACK (twilio.ts authHeader + vapi import both prefer API key, fall back to Account SID + Auth Token). The Sonika project's Auth Token IS valid (GET /Accounts.json 200, account "Sonika" active, type Trial) but number search + even /Balance return 401 "Policy evaluation failed" — a TRIAL-account policy block, not auth. So the Sonika project must be UPGRADED before it can provision; the integration itself is already proven on the upgraded GCA account (earlier successful buy→import→release used auth-token auth, the same code path). Two go-live TODOs: upgrade Sonika project + create one correctly-paired Standard key, then final on-project verify and drop the token.
27/06/2026 (e): Closed the client-removal billing leak. deleteSubAccount now DE-PROVISIONS before deleting: reads the seat (RLS-scoped), then releaseNumber (Twilio) + deletePhoneNumber (Vapi) + deleteAssistant (Vapi), THEN deletes the sub_account (seat cascades). Previously delete only removed DB rows, leaving the Twilio number billed + Vapi assistant orphaned and the handles lost. Added migration 20260627120000 (seats.vapi_phone_number_id) so the Vapi phone-number resource is also cleanable; provisionClient now stores it; new vapi.deletePhoneNumber helper; architecture.md schema synced. Teardown helpers no-op in mock mode / on mock_ ids, so dev delete stays pure-DB. Verified (Playwright, mock): provision → remove → row gone + empty state; DB confirms 0 sub_accounts / 0 seats (clean cascade, no orphan). tsc + build green.
27/06/2026 (f): Built call logs. Researched the Vapi end-of-call-report shape + X-Vapi-Secret auth via vapi-docs first. Webhook route handler src/app/api/webhooks/vapi/route.ts: verifies X-Vapi-Secret (enforced when VAPI_WEBHOOK_SECRET set), parses end-of-call-report, maps call→seat by vapi_assistant_id (fallback vapi_phone_number_id) via the service-role client, stamps agency_id/sub_account_id, idempotent upsert on vapi_call_id (Vapi retries); ignores other event types. createAssistant now sets server.url+secret on the assistant when VAPI_WEBHOOK_URL is configured (skipped locally — Vapi can't reach localhost). Proxy matcher updated to exclude /api so webhooks bypass session handling. UI: per-client page /dashboard/clients/[id] (RLS-scoped) listing calls — caller, time, duration, ended_reason, summary, expandable transcript, recording link; dashboard client names now link to it. Verified (Playwright + simulated webhook POST, mock provisioning): POST→200, idempotent re-POST keeps 1 row, page shows caller/summary/duration 1:35/recording, second agency gets 404 (RLS). tsc + build green. Note: real ingestion needs a public VAPI_WEBHOOK_URL (tunnel in dev); local verify used a simulated payload against a mock-assistant seat. Next: per-seat Stripe billing.
27/06/2026 (g): Built per-seat Stripe billing — the last MVP slice. Model (per architecture.md §6): one customer + one subscription per agency, subscription item quantity = live seat count. Migration 20260627150000 adds agencies.stripe_subscription_id / stripe_subscription_item_id / subscription_status. Stripe service src/lib/services/stripe.ts (env-gated on STRIPE_SECRET_KEY; lazy client): ensureCustomer, createCheckoutSession, createPortalSession, setSeatQuantity, getSubscription, constructWebhookEvent + isBillingEnabled/isAgencyBilled helpers. Billing actions (startCheckout/openBillingPortal) + /dashboard/billing page (status, live-seat count, Set up billing / Manage billing) + Billing nav link. Stripe webhook /api/webhooks/stripe verifies signature, syncs subscription status+item to the agency by metadata.agency_id (fallback customer id) via service role. ENFORCEMENT (chosen): provisioning requires an active/trialing subscription when billing is enabled — protects Sonika's margin (a live seat costs real Vapi+Twilio money); provision button gates to the billing page otherwise. Seat quantity synced to Stripe on every provision/remove (syncSeatBilling). DAL extended with agency billing fields. All env-gated: no STRIPE_SECRET_KEY ⇒ billing 'not configured', enforcement skipped (dev stays ungated).
  Verified: tsc + build green. Disabled path (Playwright): billing page 'not configured', provisioning ungated. Enabled path (Playwright + live Stripe TEST mode, user's sk_test_): created $49/mo per-seat price (price_1TmhNH…); checkout session returns a checkout.stripe.com URL; signed webhook → 200 + agency Active, bad signature → 400; provisioning gated pre-sub / allowed post-sub; quantity sync read back from Stripe = 2 after two provisions, 1 after removing one. Test customer/sub cleaned up. NOTE: with STRIPE_SECRET_KEY now in .env.local, billing enforcement is LIVE locally — provisioning is gated until an agency subscribes (comment the key for ungated dev). MVP COMPLETE: auth → clients → provisioning → call logs → billing.
26/06/2026: Added a BILLING_MOCK dev toggle (mirrors PROVISIONING_MOCK). Keeps Stripe fully wired — checkout, portal, webhooks, seat-quantity sync all still run — but drops the "subscribe before provisioning" paywall so you can keep a real sk_test_ key in .env.local and still provision without checking out. Extracted a generic envFlag() in mode.ts; stripe.ts gains isBillingMock() + isBillingEnforced() (= enabled AND not mocked). The two paywall checks (provisionClient guard + dashboard billingBlocked) now use isBillingEnforced(); everything else (syncSeatBilling, checkout/portal actions, billing page) still keys on isBillingEnabled(), so billing features keep working when mocked. Set BILLING_MOCK=1 in .env.local. Documented in .env.example + decisions.md. tsc green.

## Backlog (post-MVP, not yet built)
- **Edit agent / regenerate prompt.** The system prompt is generated once at provision time (Claude → stored as the Vapi assistant's system message + on seats.system_prompt) and never updated. No path to edit an agent or re-run Claude after a client's details change — today you'd remove + re-provision. Natural next feature for letting agencies tune their agents: edit the stored prompt and PATCH the Vapi assistant (and/or regenerate from an updated website).
